# Backend

See the project-level `README.md` for setup and feature documentation.

Swagger UI runs at `http://127.0.0.1:8000/docs`.

Place the trained Keras model at `model/brain_tumor_model.keras`. Without it, the API runs in clearly marked deterministic demo mode.
